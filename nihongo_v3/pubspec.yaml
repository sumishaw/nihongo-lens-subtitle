name: nihongo_lens

description: Floating Japanese subtitle translator

publish_to: 'none'

version: 2.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  permission_handler: ^11.3.1
  flutter_overlay_window: ^0.4.4
  speech_to_text: ^7.0.0
  translator: ^1.0.3+1
  flutter_tts: ^4.0.2
  avatar_glow: ^3.0.1
  cupertino_icons: ^1.0.8

flutter:
  uses-material-design: true
